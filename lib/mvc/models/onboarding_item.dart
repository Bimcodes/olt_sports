class OnboardingItem {
  final String title;
  final String description;
  final String
  imagePlaceholder; // To use as a placeholder until assets are provided

  OnboardingItem({
    required this.title,
    required this.description,
    required this.imagePlaceholder,
  });
}
